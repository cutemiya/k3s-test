apiVersion: v1
kind: Service
metadata:
  name: flight-app-all-weights
  namespace: flight-search
spec:
  selector:
    app: flight-app
    component: backend
  ports:
  - name: http
    port: 80
    targetPort: 4000
  type: ClusterIP