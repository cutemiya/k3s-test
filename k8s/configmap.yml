apiVersion: v1
kind: ConfigMap
metadata:
  name: flight-app-config
  namespace: flight-search
data:
  NODE_ENV: "production"
  PORT: "4000"
  GRAPHIQL_ENABLED: "true"
  CORS_ORIGIN: "*"
  APP_REPLICAS: "4"
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "flightdb"